[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

uniform float angle : hint_range(-1.0, 1.0, 0.1) = 1.0;

void fragment(){
	vec3 tex = texture(TEXTURE,UV).rgb;
	vec2 position = UV - vec2(0.5,0.5);
	
	
	COLOR = vec4(degrees(position),0.0,1.0);
	
}"
